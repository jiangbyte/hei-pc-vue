# API 后端地址
VITE_API_BASE_URL=http://localhost:18886
VITE_APP_NAME=Hei PC
VITE_DEVICE_ID=WEB_PC

# 导航菜单模式: static | backend | merge
#   static  - 纯前端静态导航
#   backend - 纯后端获取，不可用时降级静态
#   merge   - 静态为基础，后端覆盖/追加
VITE_NAV_MODE=static
